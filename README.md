# dotfiles

managed by stew

- run `stow <folder>` to symlink root dir
- for .config run `stow <folder> --target=$HOME/.config` (eg. for nvim)

tools:
- atuin 
- ripgrep
- stow
- yabai + skhd
- lazygit


